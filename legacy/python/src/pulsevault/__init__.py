"""LEGACY package — not the primary Pulse-Vault product.

The shipped product is the native Go GUI/CLI under ``gui-go/``.
This Python package is retired reference code only (historical V5 vault
implementation, tests, and interop). Do not treat pip install as the
install path. See ``legacy/python/README.md``.
"""

__app_name__ = "Pulse-Vault"
__version__ = "0.1.0"  # product version is Go 0.1.0; this package is legacy-only
